[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "goldie-trading-domain"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.10,<3"]

[tool.hatch.build.targets.wheel]
packages = ["src/goldie_domain"]

