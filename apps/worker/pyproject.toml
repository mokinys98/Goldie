[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "goldie-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["redis>=5.2,<7"]

[tool.hatch.build.targets.wheel]
packages = ["src/goldie_worker"]

